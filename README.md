# TimedClicker
A simple program that will run on only the correct machine UUID, that clicks on websites at a specific interval, it also worked through notifications and logs in automatically

Compiled with nuitka, into C code.
